[pytest]
testpaths = tests
# The crawler lives outside backend/ but shares app.knowledge; its engine is
# tested here so there is one place that runs.
pythonpath = . ..
addopts = -q
filterwarnings =
    error::DeprecationWarning:app.*
